# Demo Video Script Notes

## Branch Workflow Note

For this demo, merge the finished `dev` work back into `main` so the repository matches the production-ready app being shown.

Suggested narration:

> For the demo, I am merging the completed `dev` branch back into `main` so the repository reflects the production-ready state. In a normal production workflow, I would keep `dev` or staging separate from `main`, use preview or staging environment variables for validation, and only promote to `main` after review and final checks.
